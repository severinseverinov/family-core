export function FamilyCoreLogo({ className = "w-10 h-10" }: { className?: string }) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 100 100"
      fill="none"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
    >
      {/* CORNERSTONE / SHIELD (Dış Koruma - Aile) */}
      {/* Üst Yay */}
      <path
        d="M 25 35 Q 50 10 75 35"
        stroke="currentColor" 
        strokeWidth="10"
        className="text-blue-600 dark:text-blue-400"
      />
      {/* Alt Yay (Ters simetri - Sarılma hissi) */}
      <path
        d="M 25 65 Q 50 90 75 65"
        stroke="currentColor"
        strokeWidth="10"
        className="text-blue-600 dark:text-blue-400"
      />
      
      {/* THE CORE (İç Çekirdek - Teknoloji/Kalp) */}
      {/* Merkezdeki Güçlü Nokta */}
      <circle 
        cx="50" 
        cy="50" 
        r="14" 
        className="fill-orange-500" 
      />
      
      {/* CONNECTORS (Bağlantılar - Yanlardaki dikey çizgiler) */}
      <path d="M 20 40 L 20 60" stroke="currentColor" strokeWidth="8" className="text-blue-600 dark:text-blue-400 opacity-80" />
      <path d="M 80 40 L 80 60" stroke="currentColor" strokeWidth="8" className="text-blue-600 dark:text-blue-400 opacity-80" />
    </svg>
  );
}